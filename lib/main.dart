import 'package:flutter/material.dart';
import 'package:testeins/ui/home.dart';

void main() {
  runApp(const MaterialApp(
    // theme: ThemeData.from(
    //   colorScheme: ColorScheme.fromSwatch(primarySwatch: Colors.green),
    // ),
    home: ShoppingToDo(),
  ));
}